# winget installer manifest for LeaveSafe. Generated by packaging/generate.sh —
# edit the template in packaging/templates/, not this file.
# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json

PackageIdentifier: LeaveSafe.LeaveSafe
PackageVersion: "1.3.2"
InstallerType: portable
# LeaveSafe is a single file with no installer: portable is the honest type, and
# it is what lets winget put it on PATH without inventing an uninstaller.
# The command alias comes from Commands below. PortableCommandAlias does not
# belong here: the schema only defines it for files nested inside an archive,
# and `winget validate` warns on it — which the publish workflow treats as
# failure.
Commands:
  - leavesafe
ReleaseDate: "2026-08-13"
Installers:
  - Architecture: x64
    InstallerUrl: "https://github.com/atakankizilyuce/LeaveSafe/releases/download/v1.3.2/leavesafe-windows-amd64.exe"
    InstallerSha256: "f87e0f2b8f21ff7e285f6b381ddd0f66a77a00d7190087bd0c9adbed90c97f1c"
ManifestType: installer
ManifestVersion: 1.6.0
